import 'package:flutter/material.dart';

class StfOtherShadesWidget extends StatelessWidget {
  const StfOtherShadesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('halo'),
    );
  }
}
